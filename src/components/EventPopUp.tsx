import React from 'react';

const eventPopup = ({ event, id, response }) => {
    return (
        <div>
            <span className="popuptext" id="myPopup">A Simple Popup!</span>
        </div>
    );
};

export default eventPopup;
